# 🤖 TG Automation Panel

Система автоматизации Telegram аккаунтов с AI автоответчиком и записью паттернов действий.

## 🚀 Возможности

- **Управление аккаунтами** — подключение нескольких Telegram аккаунтов через Telethon сессии
- **AI Автоответчик** — автоматические ответы через Novita AI (moonshotai/kimi-k2-0905)
- **Запись паттернов** — запись и воспроизведение последовательностей действий
- **Бот знакомств** — автоматизация лайков/дизлайков в боте "Лео дай впнчик"
- **Чат интерфейс** — ручное управление диалогами с возможностью AI-генерации
- **Уведомления** — оповещения об ошибках в Telegram

## 📁 Структура проекта

```
├── backend/           # Node.js + TypeScript API сервер
│   ├── src/
│   │   ├── db/       # SQLite база данных
│   │   ├── routes/   # API роуты
│   │   ├── services/ # Бизнес логика
│   │   └── tests/    # Тесты
│   └── python/       # Telethon клиент
├── frontend/          # React + TypeScript панель управления
│   └── src/
│       ├── components/
│       ├── pages/
│       ├── stores/
│       └── lib/
└── *.session          # Telethon сессии аккаунтов
```

## ⚙️ Установка

### Backend

```bash
cd backend

# Установка зависимостей Node.js
npm install

# Установка зависимостей Python
pip install -r python/requirements.txt

# Запуск
npm run dev
```

### Frontend

```bash
cd frontend
npm install
npm run dev
```

## 🔐 Переменные окружения

### Backend (.env)

```env
NOVITA_API_KEY=your_novita_api_key
NOVITA_MODEL=moonshotai/kimi-k2-0905
TELEGRAM_BOT_TOKEN=your_bot_token
JWT_SECRET=your_secret_key
ADMIN_PASSWORD=your_admin_password
PORT=3001
SESSIONS_PATH=../
```

## 🎯 Использование

1. **Авторизация** — войдите с паролем администратора
2. **Аккаунты** — добавьте Telethon сессии в корневую папку
3. **Запуск** — активируйте нужные аккаунты
4. **AI режим** — включите автоответчик для автоматических ответов
5. **Паттерны** — записывайте и воспроизводите действия

## 🤖 AI Персонаж по умолчанию

```
ты зумер которому всегда скучно. пишешь только маленькими буквами, 
без точек в конце предложений. используешь сленг типа "чел", "кринж", 
"вайб", "рофл", "имба". отвечаешь коротко и немного лениво.
```

## 📡 API Endpoints

- `POST /api/auth/login` — авторизация
- `GET /api/accounts` — список аккаунтов
- `POST /api/accounts/:id/start` — запуск клиента
- `GET /api/patterns` — список паттернов
- `POST /api/ai/generate` — генерация AI ответа
- `WS /ws` — WebSocket для real-time событий

## 🛡️ Безопасность

- JWT токены для авторизации
- Доступ только по паролю
- Ссылка на панель через Telegram бота

## 📦 Деплой

### Backend (на сервер)

```bash
# На сервере 193.178.170.153
cd /opt/tg-automation
git clone <repo>
cd backend
npm install
npm run build
pm2 start dist/index.js --name tg-automation
```

### Frontend (Vercel)

```bash
cd frontend
npx vercel --prod
```

## 🔗 Ссылки

- **Панель управления**: https://tg-automation-panel.vercel.app
- **API**: http://193.178.170.153:3001
- **Telegram бот**: @your_bot (отправьте /start для получения ссылки)

---

**Пароль по умолчанию**: `leodaivpnchik2024`
